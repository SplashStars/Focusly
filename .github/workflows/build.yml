# ââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââ
# Focusly â GitHub Actions build workflow
# v1.1.0
#
# Triggers:
#   â¢ Push to main â builds debug APK automatically
#   â¢ Manual trigger â choose debug, release APK, or playstore AAB
#
# Artifacts produced:
#   â¢ focusly-debug-apk      (always)   â install directly on phone for testing
#   â¢ focusly-release-apk   (manual)   â unsigned release APK
#   â¢ focusly-play-store-aab (manual)  â signed AAB for Google Play upload
# ââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââ

name: Build Focusly APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build type'
        required: true
        default: 'debug'
        type: choice
        options:
          - debug
          - release
          - playstore

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # ââ 1. Get the code ââââââââââââââââââââââââââââââââââââââââââââââââââââ
      - name: Checkout code
        uses: actions/checkout@v4

      # ââ 2. Java ââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââ
      - name: Setup Java 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      # ââ 2b. Cache Gradle dependencies âââââââââââââââââââââââââââââââââââââ
      - name: Cache Gradle packages
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      # ââ 3. Flutter âââââââââââââââââââââââââââââââââââââââââââââââââââââââââ
      - name: Setup Flutter 3.22.3
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.4'
          channel: 'stable'
          cache: true

      - name: Verify Flutter version
        run: flutter --version

      # ââ 4. Android licenses ââââââââââââââââââââââââââââââââââââââââââââââââ
      - name: Accept Android SDK licenses
        run: yes | flutter doctor --android-licenses || true

      # ââ 5. Launcher icons ââââââââââââââââââââââââââââââââââââââââââââââââââ
      - name: Generate launcher icons
        run: |
          flutter create --org com.example --project-name focusly /tmp/icon_source
          cp -r /tmp/icon_source/android/app/src/main/res/mipmap-hdpi    focusly/android/app/src/main/res/
          cp -r /tmp/icon_source/android/app/src/main/res/mipmap-mdpi    focusly/android/app/src/main/res/
          cp -r /tmp/icon_source/android/app/src/main/res/mipmap-xhdpi   focusly/android/app/src/main/res/
          cp -r /tmp/icon_source/android/app/src/main/res/mipmap-xxhdpi  focusly/android/app/src/main/res/
          cp -r /tmp/icon_source/android/app/src/main/res/mipmap-xxxhdpi focusly/android/app/src/main/res/
          echo "Launcher icons ready."

      # ââ 6. Flutter packages ââââââââââââââââââââââââââââââââââââââââââââââââ
      - name: Install Flutter packages
        working-directory: focusly
        run: flutter pub get

      # ââ 6b. Decode keystore (playstore builds only) ââââââââââââââââââââââââ
      - name: Decode release keystore
        if: ${{ github.event.inputs.build_type == 'playstore' }}
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          echo "$KEYSTORE_BASE64" | base64 -d > /tmp/focusly-release.keystore
          echo "Keystore decoded: $(ls -lh /tmp/focusly-release.keystore)"

      # ââ 7a. Build debug APK ââââââââââââââââââââââââââââââââââââââââââââââââ
      - name: Build debug APK
        if: ${{ github.event_name == 'push' || github.event.inputs.build_type == 'debug' }}
        working-directory: focusly
        run: flutter build apk --debug --verbose

      - name: Upload debug APK
        if: ${{ github.event_name == 'push' || github.event.inputs.build_type == 'debug' }}
        uses: actions/upload-artifact@v4
        with:
          name: focusly-debug-apk
          path: focusly/build/app/outputs/flutter-apk/app-debug.apk
          retention-days: 30

      # ââ 7b. Build release APK (manual trigger only) âââââââââââââââââââââââ
      - name: Build release APK
        if: ${{ github.event.inputs.build_type == 'release' }}
        working-directory: focusly
        run: flutter build apk --release --verbose

      - name: Upload release APK
        if: ${{ github.event.inputs.build_type == 'release' }}
        uses: actions/upload-artifact@v4
        with:
          name: focusly-release-apk
          path: focusly/build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30

      # ââ 7c. Build signed release AAB for Google Play (playstore only) ââââââ
      - name: Build signed release AAB
        if: ${{ github.event.inputs.build_type == 'playstore' }}
        working-directory: focusly
        env:
          KEYSTORE_FILE: /tmp/focusly-release.keystore
          STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: flutter build appbundle --release --verbose

      - name: Upload AAB for Google Play
        if: ${{ github.event.inputs.build_type == 'playstore' }}
        uses: actions/upload-artifact@v4
        with:
          name: focusly-play-store-aab
          path: focusly/build/app/outputs/bundle/release/app-release.aab
          retention-days: 30

      # ââ 8. Print result ââââââââââââââââââââââââââââââââââââââââââââââââââââ
      - name: Build summary
        run: |
          echo "======================================="
          echo "  Focusly v$(grep '^version:' focusly/pubspec.yaml | cut -d' ' -f2) build complete!"
          echo "  Download the artifact from the Actions tab."
          echo "======================================="
